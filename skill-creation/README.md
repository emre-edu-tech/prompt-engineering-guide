# Creating Custom Skills
Every skill is a folder with at minimum a SKILL.md: YAML frontmatter (name, description) plus instructions, examples, and optionally scripts or reference files.

If you are using Claude AI, you can use a meta-skill called `skill-creator` to help you create a new skill. It generates the necessary folder structure and `SKILL.md` file for you.